@* This page is intentionally empty.

   It previously hosted an unauthenticated manual test harness for RichTextEditor/FileUpload that
   called real upload endpoints. Manual component verification should be done via a page temporarily
   added under an authenticated route (or removed before merge) rather than reintroducing a
   permanent, unauthenticated route here. *@
