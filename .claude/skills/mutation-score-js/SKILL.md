---
name: mutation-score-js
description: Review surviving mutations for a JS file and add/update tests to kill them. Usage: /mutation-score-js <FileName>
---

The user wants to review and fix surviving mutations for a specific JavaScript file.

## What to do

1. **Get the file name** from the user's arguments (e.g. `NebaDocument`, `NavMenu`). If no argument was given, ask the user which file they want to review.

2. **Run the mutation report formatter** to get the surviving mutations for that file:
   ```
   npm run mutation:ai:file -- <FileName>
   ```
   Capture the output — it lists each surviving mutation with: mutator type, line number, original code, mutant replacement, and whether it's covered or not covered by tests.

3. **Read the source file and its test file** so you have full context. The test file path is shown in the report output (`source.js` → `source.tests.js`).

4. **For each surviving mutation**, decide what kind of fix is needed:
   - **"not covered"** → write a new test that exercises that code path
   - **"covered, survived"** → sharpen the existing assertion to be specific enough that the mutant would fail it

5. **Follow project testing rules** (from CLAUDE.md — the user has already configured these):
   - Per CLAUDE.md: display proposed test changes in the response for review rather than inserting directly (the "show, don't insert" workflow preference). Ask the user if they want you to apply them.
   - Pragmatic priorities: `BlockStatement` > `EqualityOperator`/`LogicalOperator` > `ConditionalExpression`; deprioritize `StringLiteral` in console calls
   - Arithmetic with `0` as operand: ensure test data uses non-zero values so `a-0 ≠ a+0` ambiguity doesn't cause false survivors

6. **After showing fixes**, offer to run the mutation suite again on that file to verify kills (if the user wants).

## Notes
- The JSON report is at `reports/mutation/mutation.json` — it was generated by the last `npm run mutation:ai` run. No need to re-run Stryker unless the user asks.
- If the report is missing, tell the user to run `npm run mutation:ai` first.
- Keep fixes minimal — kill the mutation, don't refactor surrounding code.
