# Copilot Instructions

## Project Guidelines
- In Neba.Api Features, each projection should define its own DTO within its feature instead of reusing DTOs across feature/domain boundaries, to avoid coupling and shape-change breakage.